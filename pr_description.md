🎯 **What:** Removed unused `PQueue` import from `src/processor/TaskManager.js`.
💡 **Why:** Reduces dead code, improves code readability, and helps maintain a clean codebase.
✅ **Verification:** Verified with syntax checks (`node -c`) and test suite (`pnpm run ci:lint`).
✨ **Result:** Cleaned up the codebase without introducing regressions.
