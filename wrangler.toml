# wrangler.toml 已停用，'集成 lb' 已独立
# 仅保留 D1/KV 绑定参考（如果需要）

# name = "${WORKER_NAME}"
# main = "dist/lb.js"
# compatibility_date = "2023-01-01"

# KV 命名空间绑定参考
# [[kv_namespaces]]
# binding = "KV_STORAGE"
# id = "${CF_KV_NAMESPACE_ID}"
# preview_id = "${KV_PREVIEW_ID}"
