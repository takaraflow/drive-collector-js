# Local GitHub Actions secrets (use with act)
# Copy to .act.secrets and replace placeholders with real values.

BARK_WEBHOOK_URL=https://example.com/push
BARK_DEVICE_TOKEN=your_device_token

# Used by sync-manifest workflow (GitHub App credentials).
CONTRACT_REPO=owner/manifest-registry
APP_ID=123456
APP_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
# Optional token for docker/login-action (act doesn’t inject GITHUB_TOKEN).
GITHUB_TOKEN=ghp_local_dummy_token
